# 项目现状

> 更新时间：2026-07-17。本文只记录当前产品、验证和生产事实，不记录开发过程。

## 状态证据

| 层级 | 最后核验 | 结论 |
|---|---|---|
| 客户端体验审查起点 | `5aa2782ad10c4fafbacb9fe395b6973b1b95ecb4` | 以该 commit 的页面、导航和功能为体验审查基线；页面走查已结束，当前按问题批次统一修改；schema 仍为 v31 |
| 服务端本地验证 | 2026-07-17 | `npm run check` 生产编译和 PostgreSQL 18 smoke 通过；`npm run typecheck` 通过 |
| 最近 iOS 产物 | `756e8f331774699a118005437d4d63172f381c06` / run `29549647407` | Xcode 26.3 unsigned IPA 归档和下载校验通过，版本 `0.2.0 (11)` |
| 生产环境 | 2026-07-17 | 美国唯一可写源站运行 `756e8f331774699a118005437d4d63172f381c06`、schema v31；完成官方系统更新并重启到 `6.8.0-136-generic`，移除 `fwupd`、孤立依赖和废弃旧内核，Docker 构建缓存清零；核心服务与 7 个既有容器均恢复，失败单元、待更新和可自动移除包均为零，本机、公开入口与备份校验通过；日本只承担入口与中转，不保留 CoupleChat 项目备份或应用运行时，最小化清理、日志轮转修复和官方系统更新后无失败单元 |

本机旧 IPA、tar、展开 release 或备份目录不属于生产证据。服务器细节见 [SERVER.md](SERVER.md)，签名与侧载见 [IOS.md](IOS.md)。

## 产品边界

悄悄话只服务 `xu` 与 `si` 两位固定用户，支持同一账号在两台 iPhone 17 和一台 iPad 上同时登录。主导航依次为聊天、时光、大橘、计划和我的。

服务端不提供公开注册、邀请码、配对、创建情侣空间或加入其他情侣空间。情侣空间是共享数据边界；私人 AI、私人提醒和设备配置按账号隔离。

## 技术基线

- 客户端：iOS/iPadOS 26、Swift 5.9、SwiftUI + UIKit、版本 `0.2.0 (11)`。
- Bundle ID：`com.hugxu0.couplechat.native`；工程由根目录 `project.yml` 生成。
- iOS 依赖：Socket.IO Client Swift `16.1.0`、GLTFKit2 `0.5.15`，均固定精确版本。
- 3D 资源：`Sources/Resources/cute_cat.glb` 受 Git 管理并随 IPA 发布。
- 服务端：Node.js 22、Fastify 5、Socket.IO 4、PostgreSQL 16，当前 schema v31。
- 公开基地址：`https://hoo66.top`；Debug 与 Release 当前都连接该地址。
- 发布方式：公开单仓库；服务端只发布固定 SHA 的 `server/` 子树；GitHub 只生成 unsigned IPA。
- 签名方式：免费 Apple Personal Team，不支持 TestFlight/App Store，约 7 天需要刷新。

## 当前功能

### 账号、消息与同步

- 固定双账号登录、设备会话查看和撤销，多设备 presence 与严格已读。
- 共同聊天和按账号隔离的大橘私聊。
- 文字、原图、视频、语音、文件、静态/动态贴纸、引用、搜索和完整撤回。
- SQLite 账号缓存、可靠 outbox、`clientId` 幂等、历史分页、Sync V2 和 Socket 重连补拉。
- 离线可进入有界本地历史；回前台后补 bootstrap、Sync V2 和 Socket 健康检查。
- 图片/视频预览、收藏、主题、壁纸、Markdown 表格和 Mermaid。
- 语音异步转写、失败重试、历史补建和撤回级联。

### 时光、计划与大橘

- 纪念日、聊天统计、多本共同相册、分组动态和直接上传。
- 今日推荐按北京时间 06:00 切换，支持双方互荐、未读、收下和个人历史隐藏。
- 共享/私人日历、提醒、备忘、完成、删除和版本冲突处理。
- 共享提醒通知双方全部有效设备，私人提醒只通知创建者设备。
- 服务端持久化的大橘状态、互动冷却、日夜场景和 AI 私聊。

### 设置与 AI

- 深浅模式、主题、头像、设备、Bark、收藏、账号隔离的表情库和存储管理。
- `@大橘`、图片理解、联网查询、来源卡、事项确认卡和上下文摘要。
- 结构化 Memory、人物归属、纠正、忘记、来源查看和定时维护。

## 当前限制

- Live Photo 当前按静态图发送，尚未完成配对资源上传和原生预览。
- iPad 双栏聊天、照片拖放和完整键盘快捷键尚未完成。
- Memory 尚无本地离线缓存。
- “大橘日记”尚未实现；当前只有大橘状态、互动和私聊，目标设计见客户端体验审查台账 `UX-027`。
- 日历事件本身不自动推送；只有提醒事项按到期时间触发 Bark。
- 推荐变化只通过 REST、Sync V2 和 App 内角标同步，不发送 Bark。
- 清空 App 数据后，已丢失本地文件的失败媒体无法继续重传。
- 仓库不再保留客户端或服务端单元测试；iOS 依赖 CI 编译和真机验证，服务端依赖生产编译与 PostgreSQL smoke。
- unsigned IPA 仍需在用户受信设备上重新签名，三台设备需要定期覆盖安装。
- 备份只接受当前格式 v3；普通代码发布复用现行恢复基线，数据变更才走停写、完整备份和恢复验证。

## 客户端体验审查台账

本轮先从真实设备上的实际任务出发逐页记录体验问题，页面走查已经结束，当前进入统一修改阶段。修改按公共组件、状态所有权和根因分批落地；`已修复`仍只代表源码与本机自动检查完成，不代表 CI 编译或真机体验已经验收。

后续默认按功能组一次性走查，正常项合并记录；只有遇到数据风险、破坏性操作或需要明确复现条件的问题时才单独停下来确认，避免为同一卡片反复分轮提问。

审查状态只使用：`待检查`、`检查中`、`已记录`、`已复核`。`已记录`表示该页面已经完成一轮体验走查并记下发现，不表示问题已经修复。

### 全局、启动与登录

| ID | 页面或流程 | 核心任务 | 重点状态 | 状态 |
|---|---|---|---|---|
| G01 | 启动画面与会话恢复 | 从冷启动进入可用页面 | 首次启动、已有登录、离线缓存、启动失败、前后台切换 | 已记录 |
| G02 | 登录页 | 选择账号并完成登录 | 密码错误、网络失败、重复点击、键盘、加载反馈 | 已记录 |
| G03 | 主导航 | 在聊天、时光、大橘、计划、我的之间切换 | Tab 红点、返回层级、子页底栏、状态保留、iPad 侧栏 | 已记录 |
| G04 | Deep Link、通知入口与全局互动浮层 | 从外部入口到达正确任务 | 冷/热启动、重复触发、目标页切换、浮层关闭与无障碍 | 已记录 |

### 聊天

| ID | 页面或流程 | 核心任务 | 重点状态 | 状态 |
|---|---|---|---|---|
| C01 | 聊天首页 | 查看双方状态、最近消息并进入聊天 | 在线状态、连接提示、未读、空历史、快捷互动 | 已记录 |
| C02 | 我的状态面板 | 设置、编辑、删除或清除状态 | 自定义输入、长度限制、同步反馈、取消操作 | 已记录 |
| C03 | 双人聊天会话 | 阅读、定位和连续收发共同消息 | 首屏位置、分页、未读分界、已读、断线补回、键盘 | 已记录 |
| C04 | 大橘私聊会话 | 与大橘连续对话 | 回复中状态、多条回复、失败兜底、确认卡、来源卡 | 待检查 |
| C05 | 输入栏与文字发送 | 编辑、引用并发送文字 | 空文本、长文本、Markdown、发送中、失败、重试、取消引用 | 已记录 |
| C06 | 附件与媒体选择 | 发送图片、视频和文件 | 权限、批量选择、预览、压缩/原图、上传进度、超限、取消 | 已记录 |
| C07 | 语音录制与转写 | 录制、发送、播放和查看转写 | 权限、中断、取消手势、短录音、后台上传、失败重试 | 已记录 |
| C08 | 表情与贴纸面板 | 选择、管理并发送表情或贴纸 | 面板切换、动态贴纸、账号隔离、空状态、失败反馈 | 已记录 |
| C09 | 消息时间线与消息操作 | 浏览并操作不同类型消息 | 长按菜单、复制、引用、收藏、撤回、转写、相册入口 | 已记录 |
| C10 | 聊天详情 | 查看对象并进入聊天管理功能 | 备注、媒体数量、搜索、背景、双人/大橘差异 | 已记录 |
| C11 | 搜索聊天记录与按日期查找 | 找到结果并跳回原消息 | 空结果、加载、分页、日期边界、跳转定位、关闭返回 | 已记录 |
| C12 | 媒体与文件 | 汇总查看聊天附件 | 图片/视频/文件分类、空状态、打开文件、加载失败 | 已记录 |
| C13 | 全屏媒体浏览器 | 浏览、缩放、播放、下载和收藏 | 左右切换、下拉关闭、视频控制、原图、目标缩略图转场 | 已记录 |
| C14 | 聊天壁纸 | 选择、导入或删除频道壁纸 | 双频道隔离、预览、深色模式、删除当前项、失败恢复 | 已记录 |

### 时光

| ID | 页面或流程 | 核心任务 | 重点状态 | 状态 |
|---|---|---|---|---|
| M01 | 时光首页 | 浏览纪念日、统计、推荐和相册 | 首屏信息层级、加载/空/错、刷新、长页面滚动 | 已记录 |
| M02 | 我的 → 日期设置 | 统一设置在一起日期和纪念日 | 单一入口、新增、编辑、删除、日期边界、取消、同步反馈 | 已记录 |
| M03 | 今日推荐卡 | 查看或刷新双方共同推荐 | 06:00 切换、加载、刷新、防重复、兜底内容 | 已记录 |
| M04 | 给 TA 推荐 | 编辑并发送推荐 | 字数、键盘、重复提交、发送中、失败后保留草稿 | 已记录 |
| M05 | 收到推荐 | 阅读并收下对方推荐 | 自动弹出、未读数、重复出现、关闭、收下反馈 | 已记录 |
| M06 | 推荐历史 | 浏览并隐藏个人历史项 | 分页、空状态、来源区分、删除确认、跨设备刷新 | 已记录 |
| M07 | 新建相册 | 创建共同相册 | 标题/摘要、键盘、校验、重复提交、失败恢复 | 已记录 |
| M08 | 相册详情与动态时间线 | 浏览相册内容和分组动态 | 分页、封面、空状态、分组、视频 | 已记录 |
| M09 | 相册管理 | 修改标题、摘要、封面或删除相册 | 版本冲突、删除确认、并发修改、失败反馈 | 已记录 |
| M10 | 发表相册动态 | 选择多媒体、编辑文案并发表 | 草稿、拍摄时间、上传进度、取消、失败重试、重复媒体 | 已记录 |
| M11 | 编辑动态文案与移出媒体 | 修改已发表内容 | 保存中、版本变化、整组/单项边界、删除后的定位 | 已记录 |

### 大橘

| ID | 页面或流程 | 核心任务 | 重点状态 | 状态 |
|---|---|---|---|---|
| D01 | 大橘主页与 3D 场景 | 查看状态并进入互动或私聊 | 首次加载、缓存状态、模型加载失败、日夜场景、性能 | 已记录 |
| D02 | 大橘互动 | 喂食、洗澡、玩耍、摸摸和睡觉 | 冷却、连续点击、版本冲突、反馈动画、跨设备同步 | 已记录 |

### 计划

| ID | 页面或流程 | 核心任务 | 重点状态 | 状态 |
|---|---|---|---|---|
| P01 | 日程主页 | 按月或列表查看共享/私人日程 | 月份切换、日期选择、时区、完成状态、空/错/加载 | 已记录 |
| P02 | 提醒列表 | 查看、完成、编辑和删除提醒 | 共享/私人、到期/逾期、Bark 语义、同步、冲突 | 已记录 |
| P03 | 备忘列表 | 查看、完成、编辑和删除备忘 | Markdown、长内容、共享/私人、空状态、同步 | 已记录 |
| P04 | 日程编辑器 | 新建或编辑日程 | 全天、起止时间、时区、范围、校验、版本冲突 | 已记录 |
| P05 | 提醒编辑器 | 新建或编辑提醒 | 到期时间、共享范围、预览、键盘、保存失败 | 已记录 |
| P06 | 备忘编辑器与 Markdown 预览 | 编辑和预览备忘 | 编辑/预览切换、格式、草稿、长文、保存失败 | 已记录 |

### 我的

| ID | 页面或流程 | 核心任务 | 重点状态 | 状态 |
|---|---|---|---|---|
| A01 | 我的首页 | 确认账号、连接状态并进入设置 | 信息层级、连接错误、入口可发现性、滚动 | 已记录 |
| A02 | 用户与大橘头像 | 拍照、从相册选择或恢复默认头像 | 权限、裁切、上传进度、失败、取消、大图内存 | 已记录 |
| A03 | 主题样式 | 调整主题色、深浅模式和已保存壁纸 | 即时预览、系统模式、删除当前项、跨页面一致性 | 已记录 |
| A04 | 大橘与记忆 | 筛选、搜索、分页和手动整理 Memory | 无本地缓存、空/错/加载、筛选保持、刷新反馈 | 待检查 |
| A05 | Memory 详情与来源 | 查看、纠正或忘掉一条 Memory | 版本冲突、重要度、来源、删除确认、返回后刷新 | 待检查 |
| A06 | 离线通知 | 保存 Bark key 并发送测试 | 空值、脱敏、保存中、测试失败、多设备独立配置 | 已记录 |
| A07 | 设备管理 | 查看并撤销登录设备 | 当前设备识别、撤销确认、最后设备、跨设备失效 | 已记录 |
| A08 | 存储空间 | 查看缓存并下载、暂停或清理 | 容量刷新、进度、后台中断、磁盘不足、危险操作确认 | 已记录 |
| A09 | 文件管理 | 浏览和清理本地附件 | 分类、排序、大小、打开、删除、已丢失文件 | 已记录 |
| A10 | 收藏 | 浏览和取消收藏媒体 | 空状态、全屏预览、原消息失效、跨账号隔离 | 已记录 |
| A11 | 退出登录与账号切换 | 安全退出并切换账号 | 未完成 outbox、SQLite 关闭、旧任务晚到、重新登录恢复 | 待检查 |

### 跨页面专项

| ID | 专项 | 重点范围 | 状态 |
|---|---|---|---|
| X01 | 视觉与可读性 | 深色模式、Dynamic Type、文字截断、对比度、安全区 | 已记录 |
| X02 | 无障碍与动态效果 | VoiceOver、按钮标签、焦点顺序、Reduce Motion、触觉依赖 | 待检查 |
| X03 | iPad 与横屏 | 分屏、侧栏 Tab、横屏、键盘、指针、拖放和弹窗尺寸 | 待检查 |
| X04 | 网络与恢复 | 弱网、断网、超时、重连、前后台切换、缓存与重试 | 已记录 |
| X05 | 多设备一致性 | 红点、已读、共享/私人数据、Socket/Sync 重复与补回 | 已记录 |
| X06 | 权限与系统中断 | 相机、相册、麦克风、文件、通知、来电和系统弹窗 | 已记录 |

### 已完成走查记录

| 记录 ID | 页面 ID | 设备与场景 | 观察结论 | 后续 |
|---|---|---|---|---|
| R-001 | G01 | 当前测试设备；已登录后从后台完全关闭，再以正常网络重新进入 | 开屏动画约 2～3 秒；随后进入聊天首页；未出现错误、登录页闪现、页面闪烁或明显内容跳动 | 保留为当前正常基线；离线、弱网和其他设备仍待专项复核 |
| R-002 | G02、G03 | 软件安装后首次进入登录页，以及已登录后的再次启动 | 首次进入登录页时只显示密码输入框，未显示用户头像选择；完全关闭后第二次进入才显示头像选择。已登录重启后，“时光”Tab 红点偶发显示不正确，具体外观待截图 | 分别见 `UX-001`、`UX-002`；设备型号、build 和稳定复现频率待补 |
| R-003 | G03、C01 | 当前测试设备；正常网络下依次切换五个主 Tab，再返回聊天首页 | 五个 Tab 切换与状态表现正常；本轮没有未读消息，因此未出现红点。聊天首页双方头像、状态、在线信息和最新消息均正确；快捷互动容易理解，“进入聊天”按钮自然 | 当前未发现新问题；`UX-002` 保留到真实出现未读状态时再复核 |
| R-004 | C03、C05 | 当前测试设备；进入双人聊天、打开键盘并发送普通文字，同时观察收取新消息和弱网发送 | 会话正确落在最新消息，页面无突然跳动；消息顺序、时间、头像和输入栏安全区均正常。正常网络下消息立即出现并自然变为成功，未见重复；已读状态正常。弱网下发送的消息出现三个点后，即使换到可用网络且已经能够正常收到新消息，原消息仍会卡在发送中 | 键盘打开时新消息可被输入栏和键盘遮住，见 `UX-003`；网络恢复后 outbox 仍不收敛见 `UX-004` |
| R-005 | C06、C09、C13 | 当前测试设备；打开附件菜单、选择照片或视频，并观察聊天内封面和视频播放 | 菜单提供“照片或视频”和“文件”；虽然没有单独显示取消项，但点击空白处可自然关闭，取消后的输入状态正常。选择媒体后能出现本地预览，照片和视频基础选择流程可用 | 弹窗箭头锚点见 `UX-005`；收到的照片偶发无封面见 `UX-006`；视频封面与播放器视觉脱节见 `UX-007`；视频播放等待过久见 `UX-008`。文件、批量选择和失败流程仍待检查 |
| R-006 | C06、C09、C12 | 当前测试设备；发送图片和普通文件，并点击聊天中的文件消息 | 图片发送正常。文件可以选取和发送，但聊天中的文件气泡比例与视觉表达不自然，无法一眼识别为文件；没有明确下载按钮，点击气泡后直接离开 App 跳到浏览器 | 文件气泡见 `UX-009`；文件打开与下载路径见 `UX-010`。C06 本轮结束，媒体与文件汇总页仍待完整检查 |
| R-007 | C07 | 当前测试设备；检查语音录制、取消、发送、播放和转写 | 语音功能当前整体正常，未发现录制手势、发送、播放或转写方面的体验问题 | 保留为当前正常基线；权限拒绝、系统中断和弱网场景留到跨页面专项复核 |
| R-008 | C08 | 当前测试设备；检查表情面板打开与收起、键盘切换、Emoji、图片/动态表情发送和分组入口 | 表情面板当前整体正常，未发现面板切换、发送、动态效果或分组入口方面的体验问题 | 保留为当前正常基线；账号切换后的表情隔离和弱网失败留到专项复核 |
| R-009 | C09、A10 | 当前测试设备；浏览时间线，长按文字与图片，测试复制、引用、撤回和引用跳转 | 时间线浏览、复制菜单和撤回功能正常。引用消息可以发送，但引用预览会因新消息较短而过度截断；撤回提示样式需统一；图片长按只有加入共同相册而没有收藏；点击引用内容不能跳回原消息 | 分别见 `UX-011` 至 `UX-014`。收藏页面被反馈存在较大体验问题，留到 A10 专项完整记录 |
| R-010 | C10 | 当前测试设备；打开和返回聊天详情，检查身份信息、子页导航和媒体数量 | 打开与返回自然，子页中的底部 Tab 隐藏正确；头像、用户名及其他入口均正常。媒体与文件数量主要反映本机安装后已经同步或缓存到的内容，用户接受当前有界范围，不要求为了计数同步全部历史 | 当前未发现新问题；搜索、媒体与文件、壁纸分别留到 C11、C12、C14 专项检查 |
| R-011 | C03、C11 | 当前测试设备；搜索云端已有但本机安装后未缓存的早期导入消息，并搜索高频关键词 | 搜索能够返回早期云端结果，但点击后只显示目标附近约 20 条消息，无法连续向上或向下浏览；继续滑动会突然回到本机安装后已同步的近几日消息窗口。高频关键词最多只显示约 50 条结果，列表末尾没有继续加载入口 | 历史窗口连续性见 `UX-015`；搜索结果分页见 `UX-016`；按日期对照见 `R-012` |
| R-012 | C03、C11 | 当前测试设备；分别按日期打开早期云端导入消息和本机安装后已有缓存的近期消息 | 早期云端日期只能看到目标附近少量上下文，无法继续浏览；近期本地日期可以正常持续向后滑动和加载。两者差异进一步确认问题位于旧云端窗口的续接，而不是日期选择控件本身 | 合并进 `UX-015`；C11 本轮结束 |
| R-013 | C12、C13 | 当前测试设备；打开媒体与文件汇总页，查看图片、视频并进入全屏预览 | 汇总页当前只看到图片和视频，没有可见的文件入口或分类。视频继续存在起播慢和封面生硬覆盖的问题。图片全屏左右切换正常，但上下滑退出的交互与消失动画不自然 | 文件入口见 `UX-017`；视频问题继续归入 `UX-007/UX-008`；上下滑退出见 `UX-018`。共同相册的交互式缩回效果作为目标参照 |
| R-014 | C14、X01 | 当前测试设备；选择明暗区域变化较大的自定义聊天壁纸并观察输入区与按钮 | 不同半透明控件会因背后局部亮度不同而呈现不同的明暗倾向，例如输入栏整体偏白而旁边按钮近乎透明，像同时使用两套外观，界面缺少统一性 | 见 `UX-019`；聊天控件应使用统一的外观决策和表面层，不让每个控件独立受局部壁纸亮度支配 |
| R-015 | M01、M03、X01 | 当前测试设备；浏览时光首页的纪念日、聊天统计、今日推荐和个人相册 | 个人相册区域当前正常。聊天统计的白色柱体、空柱轨道与白色卡片背景容易重合，数据为空时尤其难以分辨。今日推荐顶部的类型标签、“换一个”按钮及文字偏大，内容区右侧和按钮下方留白不均，推荐内容的主次层级不够紧凑 | 统计图对比度见 `UX-020`；推荐卡布局见 `UX-021`，待脱敏截图后补充具体尺寸关系。“今天的回声”是按当前月日匹配往年共同相册媒体的条件区块，不是固定每日文案 |
| R-016 | M01、M02、M03、X01 | 当前测试设备截图与基线源码核对；复核聊天统计、今日推荐和日期设置入口 | 当前没有出现“今天的回声”，用户明确不需要该能力。截图确认聊天统计的白色空轨道与白色卡片几乎融合；推荐正文视觉重心偏左、字号偏大，顶部类型标签和“换一个”按钮也偏大。日期设置应只保留“我的 → 日期设置”入口；源码中虽然已经没有卡片右上角编辑图标，但整张“在一起第 N 天”卡片仍会打开同一编辑页 | 完整移除“今天的回声”见 `UX-022`；时光首页日期编辑遗留入口见 `UX-023`；截图细化已合并进 `UX-020/UX-021` |
| R-017 | M01、M02、M03 | 当前测试设备；横向滑动聊天统计、查看推荐标题栏按钮，并完整查看“我的 → 日期设置” | 图表最左侧的细粉线是横向分页时上一组柱体露出的边缘，用户接受其作为滑动连续性提示，不作为缺陷。推荐标题右侧的历史和礼物圆形按钮也可以略微缩小。日期设置页当前数据、编辑内容和交互均正确，未发现页面内部问题 | M02 第一轮完成；其唯一待改项仍是移除时光首页的重复入口 `UX-023`。推荐按钮尺寸补充进 `UX-021`，继续检查 M03 的刷新行为 |
| R-018 | M03 | 当前测试设备；点击今日推荐的“换一个”，并核对服务端生成路径 | 刷新成功返回了新文字，但推荐对象或主题与上一条高度相似，主要只是重新组合措辞。源码显示刷新会复用同一作息日、同一批共同 Memory 和同一提示词，不传入当前或近期推荐，也没有名称或语义去重；提示词的格式示例与内置兜底还使用了同一个具体电影，进一步形成答案锚定 | 见 `UX-024`；现象主要由生成与去重策略造成，不能仅据此判断生产所用模型能力。未连接生产环境核对 provider、日志或原始模型响应 |
| R-019 | M03 | 当前测试设备；回顾点击“换一个”时的加载与卡片变化 | 刷新时的加载反馈、卡片尺寸和内容替换过程均自然，未出现闪白、塌陷、跳动或重复点击方面的问题 | M03 第一轮完成；保留布局问题 `UX-021` 和结果去重问题 `UX-024`，开始 M04 |
| R-020 | M04 | 当前测试设备；打开“给 TA 推荐”、输入内容并成功发送 | 接收人、输入框、键盘、字数、按钮状态和实际发送均正常。发送成功后编辑页直接关闭，只有触觉反馈，没有任何可见的成功提示，用户无法明确确认推荐是否已经送达 | 见 `UX-025`；M04 第一轮完成，开始在对方设备检查 M05 |
| R-021 | M05 | 对方账号已登录设备；接收并收下一条成员推荐 | 时光 Tab 红点、自动弹出、发送人和内容、收下后的关闭与红点清除均正常；离开再返回后没有重复弹出 | M05 第一轮完成；M06 至 M11 改为一次性批量走查 |
| R-022 | M06、M07、M08、M09、M10、M11 | 当前测试设备；批量检查推荐历史，以及共同相册的创建、浏览、管理、发表、编辑与移除流程 | 推荐历史的来源、顺序、滚动和删除确认正常；相册创建、详情时间线、媒体分组、相册管理、发表动态、编辑文案和移除/删除确认均正常，未发现新问题 | 时光模块 M01 至 M11 第一轮全部完成；开始批量检查 D01、D02、C04、A04、A05 |
| R-023 | D01、D02、X01 | 当前测试设备；观察大橘主页昼夜场景并检查现有互动 | 大橘场景卡片偶尔呈现黑夜配色，而卡片外页面仍是浅色主题，视觉上像两套外观。源码确认场景在 App 为深色或设备时间不处于 06:00～19:00 时都会进入黑夜，但页面外层只跟随 App 主题。现有五种互动当前可用，本轮不扩展玩法 | 昼夜外观错位见 `UX-026`；互动玩法保留当前基线、后续另行完善。新增“大橘日记”目标设计见 `UX-027` |
| R-024 | P01、P02、P03、P04、P05、P06 | 当前测试设备；批量检查计划主页，以及日程、提醒、备忘的查看、新建、编辑、完成和相关呈现 | 计划模块本轮所检查功能均正常，未发现月份或日期浏览、共享/私人范围、编辑保存、完成状态、Markdown 预览或页面布局方面的新问题 | P01 至 P06 第一轮全部完成；开始批量检查“我的”模块，AI 私聊和 Memory 继续延期 |
| R-025 | A01、A02、A03、A06、A07、A08、A09、A10 | 当前测试设备；批量检查“我的”首页、头像、主题、离线通知、设备、存储、文件和收藏的入口与非破坏性流程 | 除头像操作菜单与收藏图片排版外，其余本轮检查内容正常。点击更换头像后，操作菜单固定从屏幕底部出现，与被点击的头像按钮缺少空间关联；收藏页图片排版不如聊天“媒体与文件”页自然 | 头像菜单见 `UX-028`；收藏排版见 `UX-029`。AI 和 Memory 继续延期；G02 采用已有首装观察，不重复退出登录，A11 留待后续安全复核，下一步检查 C02 与 G04 |
| R-026 | C02、G04 | 当前测试设备；检查聊天首页的个人状态操作，并结合已有使用过程观察通知或外部入口跳转 | 本轮未发现新的状态选择、编辑、清除或入口跳转问题 | C02 与 G04 第一轮完成；进入 X01 至 X06 跨页面专项 |
| R-027 | X01、X02、X03、X04、X05、X06 | 当前 iPhone；调大系统字体并复核常用页面，同时结合现有使用观察网络恢复、多设备与系统权限流程 | 调大字体后，部分页面文字显示不佳；其余本轮可检查项目未发现新问题。当前没有 iPad，无法进行 iPad、分屏和大屏弹窗真机测试；用户不使用 VoiceOver 与“减弱动态效果”，本轮不把这两项写成已验证 | 大字体适配见 `UX-030`；iPad 自主适配与验证边界见 `UX-031`。X02、X03 保持待检查，其余跨页面专项结束 |
| R-028 | C01、C05、M01、M03、A02、X01 | 当前 iPhone；安装并复核 `d4dd6a5` unsigned IPA，结合五张脱敏截图检查首轮统一修改 | 聊天首页五个快捷互动被四列网格拆成两行，状态文字偏大；用户与大橘头像菜单向上展开并被顶部遮挡；今日推荐的类型/刷新区仍过松且正文右侧留白不均；纪念日主数字与小卡数字层级变小；明暗混合壁纸下输入胶囊仍明显比左侧大橘按钮更白 | 快捷互动、状态、头像菜单、推荐和纪念日按截图修正；统一覆盖聊天输入区与顶部按钮玻璃色调的后续方案被用户要求撤回，`UX-019` 保留为待重新设计 |
| R-029 | C03、C05 | 当前 iPhone；位于最新位置，打开键盘并发送普通文字；`c05e836` 与 `1b178d7` 均稳定复现，随后由对方发送新消息 | 每次发送成功后仍会出现“回到最新”箭头，对方新消息不会上推到输入栏上方而是落在输入栏后面。两轮围绕 ACK 顺序和 pending id 替换的修复均未触及三处根因：输入区安全边界依赖估算；程序化布局变化被滚动回调当成用户上滑；实时来信在 SQLite 持久锚点推进前被错误判成历史窗口 | 合并回 `UX-003`；先以结构约束验证最后一条消息安全区并修正跟随状态与最新窗口判定，随后在不回退这两项状态修复的前提下，改为按输入区真实 `frame` 计算安全边界；保留 ACK/SQLite 有序提交修复 |
| R-030 | C03、C05、C14 | 当前 iPhone；安装前一轮消息跟随修复后，打开键盘连续发送，并对比当前输入区与 Telegram 原生底栏截图 | 发送消息及位于最新位置时的跟随已经正常；但底部输入胶囊和圆形按钮呈现偏白、近乎不透明，背景内容被整体截在输入区上方，缺少原生玻璃覆盖滚动内容时的连续感 | 消息跟随正常基线继续保留；原生底栏视觉见 `UX-032`，不使用自绘渐变玻璃 |

### 问题记录格式

每个问题使用独立编号 `UX-001` 起递增。先记录用户能观察到的事实，不把尚未定位的原因或解决方案写成结论。

| 问题 ID | 页面 ID | 优先级 | 设备与场景 | 实际表现 | 期望表现 | 复现步骤与频率 | 状态 |
|---|---|---|---|---|---|---|---|
| UX-001 | G02 | P1 | 软件安装后、尚未登录的首次启动；具体设备与 build 待补 | 登录页只显示密码输入框，用户头像选择区域缺失；把 App 从后台完全关闭后再次进入，头像选择区域才出现 | 首次进入登录页时就稳定显示两位用户的头像选择，并明确当前准备登录的账号 | 安装后首次打开时观察到一次；已加入固定账号区域、自动重试和手动重试，仍需重装真机验收 | 已修复 |
| UX-002 | G03 | P2 | 已登录后完全关闭 App 并重新进入聊天首页；具体设备与 build 待补 | 底部“时光”Tab 的红点偶发显示不正确；源码确认原实现用文字 `•` 模拟角标，而且只保存有无未读，系统会把它当普通文字角标排版 | 未读状态和红点样式应在每次启动后保持正确、稳定且一致 | 已移除文字伪红点，角标状态改为服务端返回的准确未读数并交给系统数字 Badge 呈现；需在 0、1 和多条未读以及冷启动后真机验收 | 已修复 |
| UX-003 | C03、C05 | P2 | 当前 iPhone；位于最新消息附近、键盘保持打开并发送普通文字；`e19549a`、`c05e836`、`1b178d7` 真机连续复现 | 点击发送后会出现“回到底部”的向下箭头；消息已显示成功对勾但仍离开底部，随后对方新消息不会上推而是落到输入栏和键盘后面 | 用户本来就在最新位置时，自己发送成功及随后收到的新消息都应稳定显示在输入栏上方；只有用户主动停留在旧消息时才显示回到最新入口 | 已改为三层不变量：最后一条消息的安全边界由输入区完成布局后的真实 `frame` 精确计算；跟随最新状态只接受真实用户滚动；当前窗口只要仍包含发布前已知的最新持久消息，随后追加的 pending、ACK 或实时来信都继续视为最新窗口。用户已确认前一轮发送和贴底恢复正常；ACK/SQLite 有序提交修复继续保留 | 已修复 |
| UX-004 | C05、X04 | P1 | 网络较差时发送文字消息，随后切换到可用网络；具体设备与 build 待补 | 消息长期只显示三个点的发送中状态；即使已经能够正常收到对方的新消息，证明连接至少部分恢复，原发送任务仍不会成功、失败或提供重试入口 | 连接恢复后 outbox 应自动继续发送并可靠收敛；若自动恢复失败，应在合理超时后明确标记失败并提供手动重试，同时保证幂等、不能重复发送 | 已加入网络路径切换重连、outbox 自动重放、ACK 硬超时和有限重试；需弱网切换真机验收 | 已修复 |
| UX-005 | C06 | P3 | 在聊天输入栏点击附件按钮；具体设备与 build 待补 | 附件弹窗底部的小箭头指向输入栏中间，没有指向实际触发它的回形针按钮 | 弹窗应从附件按钮附近自然出现，箭头准确指向触发控件；或使用不依赖错误锚点的系统呈现方式 | 已把 iPad/弹出式菜单的 source rect 改为附件按钮自身边界，箭头只允许从按钮上方向下指向；需真机验收 | 已修复 |
| UX-006 | C06、C09 | P2 | 接收对方发送的照片并在聊天时间线查看；具体设备与 build 待补 | 照片消息偶发不显示封面，用户只能知道收到媒体，却无法直接看到照片预览 | 照片消息应优先显示本地缓存或远程缩略图；加载中、失败和可重试状态需要明确区分 | 图片网络读取增加一次短退避重试，4xx 等不可恢复响应不重试；仍需在原偶发网络条件下真机验收 | 已修复 |
| UX-007 | C09、C12、C13 | P2 | 在聊天时间线、媒体汇总页或全屏浏览器查看视频；具体设备与 build 待补 | 视频封面像单独抽出后生硬覆盖在播放器上方，与播放器本身的布局和播放状态衔接不自然 | 视频封面、加载状态和系统播放器应属于同一个稳定容器，开始播放时自然切换，不应出现视觉脱节 | 全屏与媒体详情已统一为原生播放器加载/失败层，不再额外覆盖独立封面；需真机验收 | 已修复 |
| UX-008 | C12、C13 | P1 | 从聊天或媒体汇总页打开远程视频；具体设备与 build 待补 | 视频加载很慢，体验上必须等待完整下载后才能开始在线播放 | 使用系统播放器时应在服务端 Range 与网络条件允许的情况下渐进缓冲并尽快起播，同时提供原生加载和失败反馈 | 已修正 suffix Range 并取消缩略图整段下载回退，服务端冒烟通过；仍需远程视频真机验收 | 已修复 |
| UX-009 | C06、C09 | P2 | 在聊天时间线查看已经发送的文件消息；具体设备与 build 待补 | 文件气泡的尺寸和比例显得不协调，视觉上也不像文件卡片，用户难以快速识别消息类型 | 文件消息应使用稳定、紧凑的文件卡片，清楚呈现文件图标、名称、类型或大小以及可执行操作 | 已改为紧凑文件卡片，包含文件图标、名称、状态/预览说明和操作箭头；需真机验收不同文件名和大字体 | 已修复 |
| UX-010 | C09、C12 | P2 | 点击聊天中的文件消息；具体设备与 build 待补 | 气泡中没有明确的下载或打开按钮，点击后直接跳转到外部浏览器，打断聊天上下文 | 应明确区分下载、在 App 内预览、使用其他 App 打开或分享；优先使用系统文件预览并在必须离开 App 前给出清晰预期 | 聊天气泡和“媒体与文件”页均改为下载到缓存后使用系统 Quick Look 在 App 内预览，带准备中、取消和失败反馈；需真机验证具体文件格式 | 已修复 |
| UX-011 | C09 | P2 | 引用一条较长消息，再输入并发送一条较短的新消息；具体设备与 build 待补 | 新气泡宽度主要由新消息正文决定，引用预览随之变窄，只能看到原消息开头的少量文字，难以理解引用上下文 | 引用区域应有独立的合理最小宽度和稳定行数，在短回复下仍能辨认原消息主体，同时保持气泡不过度膨胀 | 引用区域改为两行并拥有独立最小宽度，短回复不再把引用压成几个字；需真机验收长中英文与大字体 | 已修复 |
| UX-012 | C09、X01 | P3 | 撤回消息后查看系统提示，并在深色与浅色外观间比较；具体设备与 build 待补 | 撤回提示的文字、颜色或层级与时间文字不够统一，深色/浅色模式下需要进一步适配 | 撤回提示与时间分隔应共享一致的辅助文字层级、动态颜色、字号和间距，并在两种外观下保持足够对比度 | 系统/撤回提示现与时间分隔共用明暗前景色和阴影策略；需在浅色、深色和自定义壁纸上真机验收 | 已修复 |
| UX-013 | C09、A10 | P2 | 长按聊天中的图片或视频；具体设备与 build 待补 | 菜单只有“加入共同相册”，没有更常用的“收藏”，操作优先级与实际需求不符 | 图片/视频长按应优先提供“收藏/取消收藏”；加入共同相册若保留，应作为次级整理操作放在更多菜单或媒体详情中 | 长按图片/视频已改为“添加到收藏/取消收藏”，旧的共同相册选择入口与仅供该入口使用的代码已移除；需真机验收菜单文案刷新 | 已修复 |
| UX-014 | C09 | P2 | 点击一条回复消息中的引用预览；具体设备与 build 待补 | 点击引用区域没有跳转或定位到被引用的原消息，也没有失败反馈 | 原消息存在时应准确定位并给出短暂高亮；原消息已撤回或未加载时应明确说明并按需加载附近历史 | 引用预览已可点击；按内存、本地数据库、共享空间 REST 的顺序查找原消息，加载上下文后定位高亮，找不到时显示失败原因；需旧云端消息真机验收 | 已修复 |
| UX-015 | C03、C11 | P1 | 从搜索结果或按日期打开一条仅存在于云端、早于本机安装时间的导入消息；具体设备与 build 待补 | 跳转后只能看到目标上下约 20 条消息，不能以该位置继续向前或向后分页；继续滑动会突然切回本机安装后已同步的近几日消息窗口，历史上下文断裂 | 以搜索或日期目标为中心建立有界窗口后，应能双向连续分页并稳定保持当前位置，不能把远端旧窗口突然替换成本地近期窗口 | 较新方向已改为联网时云端相邻页优先，失败或离线才读本地缓存；需旧云端日期双向真机验收 | 已修复 |
| UX-016 | C11 | P2 | 搜索一个出现频率较高、结果超过约 50 条的关键词；具体设备与 build 待补 | 搜索结果停在约 50 条，滑到底部没有“加载更多”、自动分页或结果已结束的说明 | 搜索结果应使用稳定分页，在列表底部加载更多或明确告知已到末尾，并避免一次载入全部历史 | 已加入 `(ts,id)` 游标、结果去重和“加载更多结果”，服务端跨页冒烟通过；需客户端真机验收 | 已修复 |
| UX-017 | C12 | P2 | 打开聊天详情中的“媒体与文件”；具体设备与 build 待补 | 页面只看到图片和视频，没有可见的文件入口、分类或此前发送的文件项目，与页面名称不一致 | 页面应明确展示文件项目，或用“媒体/文件”分段入口分别呈现；文件卡片应提供名称、类型、大小和打开方式 | 页面已增加“图片与视频/文件”分段；文件列表显示名称、发送者、时间并可进入应用内预览；需有历史文件的真机验收 | 已修复 |
| UX-018 | C13 | P2 | 全屏查看图片后向上或向下拖动退出；具体设备与 build 待补 | 左右切换正常，但纵向拖动时的跟手、缩放和消失动画不自然，退出过程与原缩略图缺少连续关系 | 参考共同相册预览：拖动时背景、缩放和位置连续响应，超过阈值后平滑缩回对应缩略图，取消拖动则自然回弹 | “媒体与文件”和“收藏”网格现已登记当前缩略图位置，复用共同相册的纵向跟手、回弹与缩回目标转场；需真机验收上下两个方向及翻页后的目标 | 已修复 |
| UX-019 | C14、X01 | P2 | 使用局部明暗差异明显的自定义聊天壁纸；`d4dd6a5` 真机截图已复现 | 输入栏和相邻按钮分别受背后区域影响，出现一个偏白、另一个近乎透明或偏深的混合效果，同一输入区内部不统一；首轮只统一明暗判定后，大胶囊仍因局部玻璃采样明显偏白 | 整个聊天控制层应由当前 App 外观或对整张壁纸的一次性判定统一选择明暗；输入栏与按钮共享稳定表面、描边和前景色，并用统一遮罩保证对比度 | 统一覆盖输入区与顶部按钮色调的方案视觉不符合预期，已按用户要求完整撤回到原生玻璃；保留截图与现象，后续另行设计，不在本轮继续改动 | 已确认 |
| UX-020 | M01、X01 | P2 | 在浅色主题的时光首页查看聊天统计，尤其是某一天数据很少或为零时；当前截图已留作视觉依据 | 蓝/粉数据只占柱体底部，剩余的白色圆角轨道与白色卡片几乎融合；零值柱会整段消失在背景中，图表结构和数据多少不容易辨认 | 数据柱、空轨道和卡片应使用分层明确的语义颜色或描边；零值仍应保留可辨认的轨道或明确空状态，并在深浅主题下保持稳定对比 | 空轨道已改用自适应主文字色的低透明度表面和描边，零值时仍可辨认；最左侧滑动露边不处理，需浅色与深色真机验收 | 已修复 |
| UX-021 | M01、M03、X01 | P2 | 在当前设备的时光首页查看电影类今日推荐；`d4dd6a5` 真机截图已再次复现 | “电影”标签和“换一个”按钮尺寸、内边距及字体偏大，和正文争夺层级；推荐正文字号偏大且视觉重心贴左，左右留白不均；首轮缩小后，顶部控制行上下仍过空、正文行宽仍显得不对称 | 类型与刷新控件应明显缩为紧凑的次级操作，历史与礼物按钮略微缩小但保留清楚的点击范围；正文继续左对齐，同时降低字号并使用均衡的左右内边距、行宽和行距 | 类型/刷新胶囊再次压缩垂直内边距，卡片控制行间距与整体内边距同步收紧；正文取消固定理想尺寸并获得完整可用行宽，需长短正文真机复核 | 已修复 |
| UX-022 | M01、M08 | P2 | 当前没有匹配往年同日媒体，因此“今天的回声”未显示；用户确认不需要该产品能力 | 当前基线仍保留条件区块、加载态、卡片组件、客户端模型与请求，以及服务端“那年今日”查询接口，未来一旦存在匹配媒体就可能重新出现 | 统一修改时完整移除“今天的回声/那年今日”的客户端呈现、状态、模型和请求，并在确认无其他调用方后删除服务端路由与查询实现，同步更新 API、架构和项目文档；不能只隐藏首页区块 | 客户端卡片、加载态、模型、仓库请求及服务端路由和查询均已删除；旧客户端仍调用该路由，因此发布时必须先更新全部设备再移除服务端兼容接口 | 已修复 |
| UX-023 | M01、M02、A01 | P2 | 在时光首页查看“在一起第 N 天”卡片，并核对当前基线源码 | 卡片右上角编辑图标已经删除，但整张卡片仍是可点击按钮，会打开 `DateEditorSheet`，无障碍文案也仍提示“轻点编辑日期”；与“我的 → 日期设置”形成重复入口 | 时光首页卡片只负责展示日期结果，不可进入编辑；日期与纪念日的增删改统一从“我的 → 日期设置”进入，并清除时光首页对应状态、弹窗和编辑提示 | 时光首页已改为纯展示卡片并移除编辑状态、弹窗和编辑提示；`DateEditorSheet` 只保留在“我的 → 日期设置”入口 | 已修复 |
| UX-024 | M03 | P2 | 在同一作息日点击“换一个”；当前推荐与刷新结果均为正常返回 | 新结果与上一条推荐高度相似，只更换部分措辞，“换一个”没有带来可感知的新选择。刷新请求复用相同日期、相同 Memory 和相同提示词，未提供当前/近期结果作为排除项；提示词示例和确定性兜底又锚定同一个具体对象，服务端也没有名称或语义相似度检查 | 刷新时应排除当前和近期推荐，优先切换具体对象，必要时连分类也切换；格式示例使用不含真实候选名的占位结构，生成后执行名称与语义去重并有限重试；模型失败时兜底也应按刷新次数或排除集合轮换，不能同日固定返回同一条 | 生成会携带最近 12 条大橘推荐作为排除项，移除具体作品示例；同一书名号/引号对象或高文本相似结果会有限重试，内置兜底也跳过近期对象；服务端静态检查已覆盖，仍需生产模型实际刷新验收 | 已修复 |
| UX-025 | M04 | P2 | 在“给 TA 推荐”编辑页提交一条有效内容并收到成功响应 | 成功后页面立即关闭，仅触发一次触觉反馈，没有 Toast、横幅或其他可见确认；触觉关闭或没有留意时，用户无法判断是发送成功、意外关闭还是操作中断 | 成功关闭编辑页后，应在时光首页短暂显示“已推荐给 TA”之类的非阻塞成功提示；提示不能遮挡内容，也不能因重复回调出现多次 | 成功后时光首页顶部显示约 2.4 秒的非阻塞成功横幅，重复提交会取消上一计时任务并刷新提示；需真机验收关闭动画后的可见时长 | 已修复 |
| UX-026 | D01、X01 | P2 | App 使用浅色外观，并在 19:00～次日 06:00 查看大橘主页；具体设备与 build 待补 | 页面外层保持浅色主题，但场景卡片因时间条件被强制切成黑夜渐变；昼夜判断同时受 App 外观和设备小时控制，导致浅色页面包着深色场景，且切换时机依赖视图重绘，整体不统一 | 大橘页应使用单一、可预测的场景外观来源：优先跟随用户选择的 App 明暗模式，不能在明确浅色外观下仅因时间自动把卡片切黑；若以后保留独立昼夜玩法，应让整个场景容器协调过渡并在前台与时间边界主动刷新 | 场景昼夜现只跟随 App 的浅色/深色外观，已移除设备小时这一套并行判断；晚间浅色与白天深色仍需真机验收 | 已修复 |
| UX-027 | D01 | P2 | 目标设计，当前尚未实现 | 大橘主页当前没有“大橘日记”卡片，也没有按日生成、持久化和多设备同步的日记数据 | 在大橘页底部增加“大橘日记”：每天为上一个自然日生成一篇双方相同的短日记，以大橘第一人称温和观察两位主人昨天的共同聊天，不评价关系、不做心理诊断、不编造未发生内容；只读取 `couple` 共同聊天，绝不读取任一账号的 `ai` 私聊。每个日期只生成并固定一篇，不提供“换一个”改写历史；无聊天、生成中、失败与重试均有明确状态。目标实现需要服务端持久化、REST、Sync V2 和只追加 migration，并禁止在日志或 AI Trace 保存完整聊天输入 | 用户已确认产品方向；生成时点、正文长度和是否提供历史列表留到统一设计批次确定 | 已确认 |
| UX-028 | A02 | P3 | 当前测试设备；在“我的”页点击用户头像或大橘头像的更换按钮；`d4dd6a5` 真机截图已复现顶部遮挡 | 更换头像的操作菜单最初从屏幕底部出现；首轮改成锚定 Popover 后箭头边缘方向写反，菜单改为向上展开并被状态栏/顶栏遮挡 | 改为与头像按钮具有明确空间关系的紧凑菜单，从触发按钮下方展开并由系统避让底部；同时保留相册、拍照、恢复默认和取消等操作 | 用户头像、相机按钮和大橘头像的 Popover 箭头均改到浮层顶部，使内容从触发项下方向下展开；需 iPhone/iPad 真机复核系统避让 | 已修复 |
| UX-029 | A10 | P2 | 当前测试设备；在“我的 → 收藏”浏览已收藏图片 | 收藏页的图片网格密度、间距或整体排版不够自然，与聊天详情中的“媒体与文件”图片列表相比明显不协调 | 收藏页复用或对齐“媒体与文件”页的网格规则，包括列数、间距、边缘留白、缩略图比例、视频标记和加载状态，两个媒体集合页保持一致 | 收藏页已复用“媒体与文件”的自适应方形网格、间距和视频缩略图/播放标记规则；需现有收藏内容真机验收 | 已修复 |
| UX-030 | X01 | P2 | 当前 iPhone；在系统设置中调大字体后浏览常用页面；`d4dd6a5` 回归截图覆盖聊天首页与纪念日 | 部分页面文字出现显示不完整、换行或布局不协调；首轮适配又过度改变了固定视觉组件：五个快捷互动被拆成 4+1 两行，状态字和纪念日数字层级也偏离原设计 | 正文和长内容继续采用可缩放语义字体；固定数量的五个快捷互动始终保持同一行，短状态文字允许轻微缩放但不放大组件，纪念日核心数字保持原视觉层级 | 五个互动恢复单行等宽布局，短标题与状态文字恢复紧凑固定视觉档并保留无障碍标签；纪念日主数字恢复 64pt，小卡数字恢复 26pt，其他正文的 Dynamic Type 适配不回退 | 已修复 |
| UX-031 | X03 | P2 | 目标设计；本轮没有 iPad 真机，无法直接观察 iPad、分屏和大屏弹窗表现 | 当前产品已知尚未完成 iPad 双栏聊天、照片拖放和完整键盘快捷键；源码中针对横向尺寸变化的显式自适应布局仍较少，不能据此声称现有页面已适配 iPad | 统一修改时由代码侧主动检查五个主 Tab、聊天键盘、网格列数、内容最大宽度、横竖屏、分屏、弹窗锚点和指针/键盘操作；先用 iPad 模拟器与编译验证覆盖尺寸，仍将真机手感明确保留为未验证，不能伪报通过 | 五个主 Tab 已增加 760～980pt 可读内容宽度，聊天时间线以 820pt 居中，日历在空间与字号允许时双栏，媒体/壁纸/互动网格使用自适应列，主要 Sheet 使用系统 Form 尺寸，根 Tab 保留系统 sidebarAdaptable；高级双栏聊天、照片拖放和完整快捷键仍按“当前限制”单列，且本轮没有 iPad 模拟器或真机验收 | 已修复 |
| UX-032 | C03、C05、C14 | P2 | 当前 iPhone；消息跟随问题修复后，在自定义壁纸上打开键盘并观察底部输入区，同时与 Telegram 原生底栏对比 | 输入胶囊和左右按钮偏白、近乎不透明；控件分别创建常规玻璃并强制叠加白/黑 `tint`，且滚动内容没有从其后方经过，因此系统材质缺少可采样的连续背景 | 使用 iOS 26 原生清透 Liquid Glass：同一输入行由系统玻璃容器协调，普通状态不强制加明暗 `tint`，聊天内容可在视觉上滚过底栏后方；最后一条仍稳定停在输入栏上方。不得添加自绘渐变或伪玻璃；开启“降低透明度/增强对比度”时允许系统按无障碍设置变得更实 | 输入胶囊、两侧按钮和“回到最新”按钮已切换为原生 clear glass，输入行由 `UIGlassContainerEffect` 协调，底栏接入系统滚动边缘交互；时间线铺满屏幕后以输入区真实 `frame` 计算安全 inset，保留 `UX-003` 的用户滚动意图和最新窗口修复；需新 IPA 真机验收 | 已修复 |

优先级定义：`P0` 为数据丢失、安全或隐私问题；`P1` 为核心任务无法完成；`P2` 为明显阻力、错误反馈或高频困扰；`P3` 为视觉、文案和低频细节。问题状态使用 `待确认`、`已确认`、`待统一修改`、`已修复`、`已验收`。`已修复`只表示对应实现已经落到当前源码并完成本机可运行的自动检查，不代替 CI 编译或真机体验；只有复现路径在目标设备上通过后才标记`已验收`。

记录时至少说明：使用的设备类型、页面入口、原本想完成的任务、实际发生的现象、期望行为、是否稳定复现。截图和示例必须脱敏，不记录完整私聊、AI prompt、Bark key、媒体签名 URL、设备标识或其他凭据。

### 建议走查顺序

1. `G01 → G03 → C01 → C03 → C05`：先在不改变本地数据的前提下走通启动、导航和文字消息主链路。
2. `C06 → C07 → C08 → C09 → C10 → C14`：再覆盖聊天媒体、语音、贴纸和管理能力。
3. `M01 → M11`：完整走查时光、推荐和相册。
4. `D01 → D02 → C04 → A04 → A05`：覆盖大橘互动、AI 私聊和 Memory。
5. `P01 → P06`：覆盖日程、提醒、备忘及共享/私人边界。
6. `A01 → A10`：完成设备、通知、存储和个性化设置。
7. 确认云端同步完成、重要媒体已有备份且 outbox 为空后，再组合检查 `A11 + G02` 的退出、登录和账号切换。
8. 最后集中执行 `X01 → X06`，并在两台 iPhone 17 和一台 iPad 上复核关键路径。

## 已知问题

### 当前使用风险

- **SYNC-002 同毫秒分页**：`/api/messages` 仍以毫秒 `ts` 作为方向边界，同一毫秒多条消息可能重复、停滞或跳过；目标是 `(ts,id)` 复合游标或服务端序号。
- **IOS-003 开发构建连接生产**：没有独立开发服务前，开发构建不能执行破坏性操作或批量写入。
- **IOS-004 大文件主线程读取**：附件、壁纸和部分媒体缓存仍可能使用 `Data(contentsOf:)`，最大 50 MB 时可能造成卡顿和内存峰值。
- **IOS-007 账号切换竞态**：登出、SQLite close、下一账号 open 和 Socket 任务尚无统一 session generation，旧任务晚到可能影响新账号状态。
- **UPLOAD-001 临时文件**：崩溃遗留的 `.uploading` 文件缺少完整 lease/超时判定，备份遇到不安全文件时会停止。

### 生产安全风险

- **SEC-001 认证滥用防护**：可信代理、边缘与应用限流、退避和压力边界仍需在私有环境核验。
- **SEC-002 密钥轮换**：会话、内部调用和媒体用途尚未完全拆分，轮换前必须确认设备会话和历史媒体兼容性。
- **SEC-003 媒体与日志**：短期媒体授权、两层 Nginx/应用日志脱敏和保留期仍需继续核对。
- **OPS-002 离机备份**：公开仓库不能证明供应商、容量、最近成功批次或恢复演练；数据变更和冷切换前必须在私有 runbook 核验。
- **OPS-003 数据库回滚**：migration 只前进，应用镜像回滚不等于数据库回滚；schema 变化后必须使用兼容镜像或同批次数据库、uploads 和必要密钥恢复。

## 长期固定选择

- 客户端与服务端保持单仓库，协议变化在同一提交完成。
- 日本是公开入口和中转，不运行 CoupleChat；美国是唯一可写应用与数据主机，离机备份不放在日本。
- 仓库保持公开，但生产凭据、私聊、数据库、备份、签名资料和 UDID 永不进入 Git。
- iOS 最低版本保持 26，当前继续使用免费 Personal Team；需要更多设备或稳定分发时再评估 Apple Developer Program。

## 验证基线

服务端改动至少运行：

```powershell
cd server
npm run check
```

iOS 改动至少通过 GitHub Actions generic iOS 编译。交互、媒体、音频、签名、通知和多设备行为仍需在三台真机验证。具体开发流程见 [DEVELOPMENT.md](DEVELOPMENT.md)。
