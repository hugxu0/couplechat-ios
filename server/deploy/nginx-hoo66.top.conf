server {
    listen 80;
    server_name hoo66.top;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name hoo66.top;

    # Use your existing certbot paths if they differ.
    ssl_certificate /etc/letsencrypt/live/hoo66.top/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/hoo66.top/privkey.pem;

    client_max_body_size 80m;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /socket.io/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # Socket.IO ping 间隔为 25s；移动端后台/唤醒可能长时间暂停，代理保持 5 分钟余量。
        proxy_connect_timeout 10s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        proxy_buffering off;
        proxy_request_buffering off;
    }
}
